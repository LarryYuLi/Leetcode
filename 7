class Solution {
    public int reverse(int x) {
        double rev = 0;
        while(0!=x){
        	int a = x%10;
			x /= 10;
			
			rev = rev * 10 + a;
        }
		if(rev<Integer.MIN_VALUE || rev>Integer.MAX_VALUE){
			return 0;
		}
		return (int)rev;
    }
}
